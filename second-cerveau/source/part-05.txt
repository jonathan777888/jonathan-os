function renderLegacyLinks(){const box=$('legacyLinkList');if(!box)return;const arr=Array.isArray(state.linkArchive)?state.linkArchive:[];if(!arr.length){box.innerHTML='<div class="muted">Aucun ancien lien retrouvé pour l’instant. Clique sur « Rechercher les anciens liens ».</div>';return}const nodes=flattenTree(state.tree).map(x=>x.node);const opts=nodes.map(n=>`<option value="${esc(n.id)}">${esc(n.title)}</option>`).join('');box.innerHTML=arr.slice().sort((a,b)=>a.title.localeCompare(b.title,'fr')).map(e=>{const target=bestTargetForLink(e);const sourceText=(e.sources||[]).map(s=>s.label+(s.time?' · '+s.time:'')).join(' | ');return `<div class="item"><strong>${esc(e.title)}</strong><small style="word-break:break-all"><a class="linkbtn" href="${esc(e.url)}" target="_blank" rel="noopener">${esc(e.url)}</a><br>Source : ${esc(sourceText||'ancienne sauvegarde')}${e.restoredAt?'<br>Restauré : '+esc(e.restoredAt):''}</small><div class="row" style="margin-top:8px"><select class="field" data-link-target="${esc(e.id)}" style="min-width:220px">${opts}</select><button class="btn" data-open-old-link="${esc(e.id)}">Ouvrir</button><button class="btn primary" data-restore-link="${esc(e.id)}">Restaurer ce lien</button></div></div>`}).join('');arr.forEach(e=>{const sel=document.querySelector(`[data-link-target="${CSS.escape(e.id)}"]`);const target=bestTargetForLink(e);if(sel&&target)sel.value=target.id});document.querySelectorAll('[data-open-old-link]').forEach(b=>b.onclick=()=>{const e=state.linkArchive.find(x=>x.id===b.dataset.openOldLink);if(e)window.open(e.url,'_blank','noopener')});document.querySelectorAll('[data-restore-link]').forEach(b=>b.onclick=()=>{const e=state.linkArchive.find(x=>x.id===b.dataset.restoreLink);if(!e)return;const sel=document.querySelector(`[data-link-target="${CSS.escape(e.id)}"]`);const target=findNode(sel?.value);if(!target)return;const old=String(target.link||'').trim();if(old&&old!==e.url&&!confirm(`Le bloc « ${target.title} » contient déjà un autre lien. Le remplacer ?`))return;pushHistory();target.link=e.url;e.restoredAt=new Date().toISOString();e.restoredTo=target.id;selected=target.id;save(false);renderMap();renderLegacyLinks();$('linkRecoveryStatus').textContent=`Lien restauré dans « ${target.title} ». Une sauvegarde a été créée avant le changement.`})}
$('recoverLinks').onclick=()=>{const n=recoverLegacyLinks(state);save(n>0);renderLegacyLinks();$('linkRecoveryStatus').textContent=n?`${n} ancien${n>1?'s':''} lien${n>1?'s':''} ajouté${n>1?'s':''} à l’archive.`:`Aucun nouveau lien trouvé. Les anciennes données peuvent être sur un autre domaine, navigateur ou appareil.`};
function renderHistory(){let h=readJson(HIST,null);if(!Array.isArray(h))h=readJson(LEGACY_HIST,[]);if(!Array.isArray(h))h=[];$('backupList').innerHTML=h.map((x,i)=>`<div class="item"><strong>${new Date(x.at).toLocaleString('fr-CA')}</strong><small>${esc(x.appVersion||x.state?.meta?.appVersion||'ancienne version')} · sauvegarde locale #${i+1}</small><button class="btn" data-restore="${i}" style="margin-top:7px">Restaurer</button></div>`).join('')||'<div class="muted">Aucun historique.</div>';document.querySelectorAll('[data-restore]').forEach(b=>b.onclick=()=>{if(!confirm('Restaurer cette sauvegarde ?'))return;const hist=h;state=normalize(hist[+b.dataset.restore].state,false);state.meta=state.meta||{};state.meta.appVersion=APP_VERSION;state.meta.schemaVersion=SCHEMA_VERSION;selected='jesus';save();renderAll()})}
const RELEASES=[
{v:'V13',note:'V13 part directement de V12 : carte extensible, grand espace de travail, recentrage sur Jésus et agrandissement automatique lorsque les blocs approchent des bords.'},
{v:'V12',note:'V12 part directement de V11 : récupération des anciens liens Internet, archive des sources et restauration ciblée vers un bloc sans écraser les données sans confirmation.'},
{v:'V11',note:'Continuité stricte : V11 part de V10, sauvegarde avant migration, clé stable, historique de versions et aucune régression volontaire.'},
{v:'V10',note:'Récupération des anciens bilans V6/V7 et liste des bilans par date.'},
{v:'V9',note:'Correction de la sélection des blocs déplaçables et de la suppression.'},
{v:'V8',note:'Ajout de la suppression directe des blocs avec sauvegarde préalable.'},
{v:'V7',note:'Base du Second Cerveau : carte, mémoire, contexte, tâches, relations et bilans.'}
];
function renderVersions(){const el=$('versionList');if(!el)return;el.innerHTML=RELEASES.map((r,i)=>`<div class="item"><strong>${r.v}${i===0?' · actuelle':''}</strong><small>${esc(r.note)}</small></div>`).join('');const vs=$('versionStatus');if(vs)vs.textContent=`Version actuelle V13 · données schema ${SCHEMA_VERSION} · cette version hérite directement de V12.`}
function renderAll(){renderMap();renderMemories();renderTasks();loadReview();renderReviewList();renderHistory();renderLegacyLinks();renderVersions()}
renderHistory();renderLegacyLinks();renderVersions();renderMap();requestAnimationFrame(()=>centerOnNode('jesus','auto'));if(recoveredLinksOnStart&&$('linkRecoveryStatus'))$('linkRecoveryStatus').textContent=`${recoveredLinksOnStart} ancien${recoveredLinksOnStart>1?'s':''} lien${recoveredLinksOnStart>1?'s':''} retrouvé${recoveredLinksOnStart>1?'s':''} automatiquement.`;
document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.toggle('active',x===b));document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));$('page-'+b.dataset.page).classList.add('active');if(b.dataset.page==='backup'){renderHistory();renderLegacyLinks();}if(b.dataset.page==='tasks')renderTasks();if(b.dataset.page==='memory')renderMemories();if(b.dataset.page==='review'){recoverLegacyReviews(state);renderReviewList();loadReview()}});
document.addEventListener('keydown',e=>{const tag=document.activeElement?.tagName;if(tag==='INPUT'||tag==='TEXTAREA'||tag==='SELECT')return;if((e.key==='Delete'||e.key==='Backspace')&&selected!=='jesus'){e.preventDefault();deleteBlock(selected)}});
})();
