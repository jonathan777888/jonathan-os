function corpus(){const c=[];flattenTree(state.tree).forEach(({node,depth})=>c.push({kind:'Branche',title:node.title,text:[node.body,node.link,(node.tags||[]).join(' ')].join(' '),ref:node.id,depth}));state.memories.forEach(m=>c.push({kind:'Mémoire',title:m.title,text:[m.domain,m.content,(m.tags||[]).join(' ')].join(' '),ref:m.id}));state.tasks.forEach(t=>c.push({kind:'Tâche',title:t.title,text:[t.domain,t.due,t.status,findNode(t.nodeId)?.title||''].join(' '),ref:t.id}));Object.entries(state.reviews).forEach(([date,r])=>c.push({kind:'Bilan',title:'Bilan '+date,text:[r.worked,r.notWorked,r.improve].join(' '),ref:date}));return c}
function tokens(s){return norm(s).split(/[^a-z0-9]+/).filter(x=>x.length>2)}function search(q){const qt=tokens(q);return corpus().map(x=>{const hay=norm(x.title+' '+x.text);let score=0;qt.forEach(t=>{if(hay.includes(t))score+=2;if(norm(x.title).includes(t))score+=3});return{...x,score}}).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).slice(0,8)}
$('searchContext').onclick=()=>{const q=$('contextQuery').value.trim();if(!q)return;const rs=search(q);$('searchResults').innerHTML=rs.map(r=>`<div class="result"><span class="kind">${esc(r.kind)}</span><h4>${esc(r.title)}</h4><p>${esc(r.text.slice(0,220))}</p></div>`).join('')||'<div class="muted">Aucun contexte trouvé.</div>';let synth=`Question : ${q}\n\nContexte retrouvé :\n`;if(rs.length)rs.slice(0,5).forEach((r,i)=>synth+=`${i+1}. [${r.kind}] ${r.title} — ${r.text.slice(0,180)}\n`);else synth+='Aucun élément pertinent dans la mémoire actuelle.\n';const today=state.reviews[localDate()];if(today)synth+=`\nBilan d'aujourd'hui :\n- Marché : ${today.worked||'—'}\n- Pas marché : ${today.notWorked||'—'}\n- À améliorer : ${today.improve||'—'}\n`;const open=state.tasks.filter(t=>t.status!=='done').slice(0,5);if(open.length)synth+='\nTâches ouvertes :\n'+open.map(t=>'- '+t.title+(t.due?' ('+t.due+')':'')).join('\n');$('contextBox').textContent=synth};
function renderTasks(){$('taskList').innerHTML=state.tasks.map(t=>{const n=findNode(t.nodeId);return`<div class="item taskline ${t.status==='done'?'done':''}"><input type="checkbox" data-donetask="${t.id}" ${t.status==='done'?'checked':''}><div><strong>${esc(t.title)}</strong><small>${esc(t.domain||'')}${t.due?' · '+esc(t.due):''}${n?' · '+esc(n.title):''}</small></div><div><a class="linkbtn" target="_blank" rel="noopener" href="${calendarUrl(t)}">Agenda</a> <button class="btn" data-deltask="${t.id}" style="min-height:30px">×</button></div></div>`}).join('')||'<div class="muted">Aucune tâche.</div>';document.querySelectorAll('[data-donetask]').forEach(c=>c.onchange=()=>{const t=state.tasks.find(x=>x.id===c.dataset.donetask);if(t)t.status=c.checked?'done':'open';save();renderTasks()});document.querySelectorAll('[data-deltask]').forEach(b=>b.onclick=()=>{state.tasks=state.tasks.filter(t=>t.id!==b.dataset.deltask);save();renderTasks()})}
function calendarUrl(t){const d=(t.due||localDate()).replaceAll('-','');const title=encodeURIComponent(t.title);const details=encodeURIComponent('Second Cerveau IA'+(t.domain?' — '+t.domain:''));return`https://calendar.google.com/calendar/render?action=TEMPLATE&text=${title}&dates=${d}/${d}&details=${details}`}
$('addTask').onclick=()=>{const title=$('taskTitle').value.trim();if(!title)return;state.tasks.push({id:uid(),title,domain:$('taskDomain').value.trim(),due:$('taskDue').value,nodeId:$('taskNode').value,status:'open',createdAt:new Date().toISOString()});$('taskTitle').value='';save();renderTasks()};renderTasks();
function exportState(){const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='second-cerveau-v12-'+localDate()+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}$('exportBtn').onclick=exportState;$('snapshotBtn').onclick=()=>{save(true)};$('importInput').onchange=async e=>{const f=e.target.files?.[0];if(!f)return;try{const obj=JSON.parse(await f.text());state=normalize(obj,false);state.meta=state.meta||{};state.meta.appVersion=APP_VERSION;state.meta.schemaVersion=SCHEMA_VERSION;selected='jesus';save(true);renderAll();alert('Sauvegarde importée.')}catch(err){alert('Fichier JSON invalide.')}e.target.value=''};
function linkKey(title,url){return norm(title)+'|'+String(url||'').trim().toLowerCase()}
function isUsefulLink(v){const x=String(v||'').trim();return !!x&&x!=='#'&&!x.toLowerCase().startsWith('javascript:')}
function addArchivedLink(archive,title,url,nodeId,source,sourceTime){if(!isUsefulLink(url))return 0;url=String(url).trim();title=String(title||'Bloc sans titre').trim()||'Bloc sans titre';const key=linkKey(title,url);let e=archive.find(x=>linkKey(x.title,x.url)===key);if(!e){e={id:'link-'+Math.random().toString(36).slice(2,10),title,url,nodeId:nodeId||'',sources:[],firstFoundAt:new Date().toISOString(),restoredAt:null,restoredTo:null};archive.push(e)}const label=String(source||'ancienne sauvegarde');if(!e.sources.some(x=>x.label===label)){e.sources.push({label,time:sourceTime||''});return 1}return 0}
function scanTreeLinks(root,archive,source,sourceTime){let added=0;if(!root||typeof root!=='object')return 0;const walk=n=>{if(!n||typeof n!=='object')return;if(isUsefulLink(n.link))added+=addArchivedLink(archive,n.title,n.link,n.id,source,sourceTime);(n.children||[]).forEach(walk)};walk(root);return added}
function scanLegacyObject(obj,archive,source,sourceTime,depth=0){if(!obj||depth>5)return 0;let added=0;if(Array.isArray(obj)){obj.forEach((x,i)=>{added+=scanLegacyObject(x,archive,source+' #'+(i+1),x?.at||x?.time||sourceTime,depth+1)});return added}if(typeof obj!=='object')return 0;if(obj.tree)added+=scanTreeLinks(obj.tree,archive,source,obj.updatedAt||sourceTime);if(obj.map)added+=scanTreeLinks(obj.map,archive,source,obj.exportedAt||sourceTime);if(obj.id&&obj.title&&Array.isArray(obj.children))added+=scanTreeLinks(obj,archive,source,sourceTime);if(obj.state)added+=scanLegacyObject(obj.state,archive,source,obj.at||sourceTime,depth+1);if(obj.data)added+=scanLegacyObject(obj.data,archive,source,obj.time||obj.at||sourceTime,depth+1);return added}
function recoverLegacyLinks(s){s.linkArchive=Array.isArray(s.linkArchive)?s.linkArchive:[];let added=0;const known=['jonathan-racines-editable-v5','jonathan-racines-editable-v4','jonathan-racines-editable-v3','jonathan-racines-editable-v2','jonathan-racines-editable-v1','second-brain-v6','carte-racines-editable-v6','carte-racines-editable-v5','carte-racines-editable-v4',LEGACY_STATE_KEY,LEGACY_HIST,LEGACY_MAP_HIST,'second-brain-pre-upgrade-v11','second-brain-pre-upgrade-v12'];const keys=new Set(known);try{for(let i=0;i<localStorage.length;i++){const k=localStorage.key(i);if(k&&(k.includes('jonathan-racines')||k.includes('second-brain')||k.includes('carte-racines')))keys.add(k)}}catch(e){}keys.delete(KEY);keys.delete(HIST);for(const k of keys){try{const raw=localStorage.getItem(k);if(!raw)continue;const obj=JSON.parse(raw);added+=scanLegacyObject(obj,s.linkArchive,k,'',0)}catch(e){}}return added}
function bestTargetForLink(entry){const nodes=flattenTree(state.tree).map(x=>x.node);return nodes.find(n=>entry.nodeId&&n.id===entry.nodeId)||nodes.find(n=>norm(n.title)===norm(entry.title))||null}
